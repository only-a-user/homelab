# Homelab

This homelab is my dedicated k8s cluster. The repo includes the applications that are deployed via FluxCD, the configuration for Terraform, which is used to provision the VMs on my Proxmox cluster, as well as the base components, that are not managed by FluxCD.
