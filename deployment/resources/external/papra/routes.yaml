apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: external-papra-http
  namespace: external-routes
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`docs.steppler.cloud`)
      kind: Rule
      services:
        - name: external-papra-http-svc
          port: 80
      middlewares:
        - name: external-papra-http-headers
  tls:
    secretName: "letsencrypt-wildcard-cert-prod"
