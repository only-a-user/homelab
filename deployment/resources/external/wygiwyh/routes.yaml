apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: external-wygiwyh-http
  namespace: external-routes
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`finances.steppler.cloud`)
      kind: Rule
      services:
        - name: external-wygiwyh-http-svc
          port: 80
      middlewares:
        - name: external-wygiwyh-http-headers
  tls:
    secretName: "letsencrypt-wildcard-cert-prod"
