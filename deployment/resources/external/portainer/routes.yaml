apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: external-portainer-http
  namespace: external-routes
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`manage.steppler.cloud`)
      kind: Rule
      services:
        - name: external-portainer-http-svc
          port: 80
      middlewares:
        - name: external-portainer-http-headers
  tls:
    secretName: "letsencrypt-wildcard-cert-prod"
